# Copy to .env and update values
PORT=4000
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=gestion_universitaire
JWT_SECRET=replace_with_a_strong_secret
REFRESH_TOKEN_SECRET=replace_with_a_refresh_secret
JWT_EXPIRES_IN=15m
REFRESH_EXPIRES_IN=7d

# If using XAMPP default MySQL: host=127.0.0.1, user=root, password empty (unless you set one)
